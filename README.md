# SigmaForge

Detection-as-code: a repository of Sigma detection rules with a CI pipeline
that lints them, validates their metadata, tests them against sample event
data, converts them to Elasticsearch queries, and deploys them to an Elastic
Stack. Rules that fail any gate do not merge.

Full documentation lands in Phase 10.
